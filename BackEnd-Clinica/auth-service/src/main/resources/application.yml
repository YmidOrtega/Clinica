server:
  port: 8086

spring:
  application:
    name: auth-service
  flyway:
    location: classpath:db/migration/auth

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  # Configuración para RS256 (recomendado para producción)
  algorithm: RS256

  # Configuración para HS256 (fallback)
  # algorithm: HS256
  # secret: tu-secreto-super-seguro-de-al-menos-32-caracteres-aqui

  # Tiempo de expiración en segundos (1 hora por defecto)
  expiration: ${JWT_EXPIRATION}